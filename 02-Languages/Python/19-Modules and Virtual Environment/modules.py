# First install the module in the computer from pip (playstore of python)
# Then we can import the modules
# Modules are nothing but python programms
import os
import math as m    # imaporting a module with different name
from tkinter import messagebox  # importing specific things from a module
import functions as func  # importing my custom module 
# When we import a module it will run the entire py file of the module so if the file contain some print statements or function call it will run em as well keep them in mind

print(func.a)
print(__name__)